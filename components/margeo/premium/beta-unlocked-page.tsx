"use client";

import { RetourHubPage } from "@/components/margeo/retour/retour-hub-page";

/**
 * Ancienne page « Bêta privée / Fonctionnalités débloquées ».
 * En mode bêta, /premium affiche désormais la section Retour complète.
 */
export function BetaUnlockedPremiumPage() {
  return <RetourHubPage />;
}
